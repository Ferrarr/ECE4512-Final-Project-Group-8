# only import classify since that's the only function we need from Classifier in main
from Classifier import classify
import Enhance as en


def main():
    # ask for image, once image is passed, run Classfier.classify()
    # Classifier.classify()
    # depending on the type of noise, run through several denoising functions
    # for example if its raining and low light then we do
    # en.derain(image)
    # en.brighten(image)

    return None
