import cv2 as cv

def blur(image):
    cv.imshow("Title", image)
    cv.waitKey(0)
    cv.destroyAllWindows()

    return ""

# image degradation classifier, outputs degradation types
def classify(plate):
    degradations = []

    return degradations
